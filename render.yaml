services:
  - type: web
    name: insightedge-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd career_api && uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
